AI 驱动的自动化数据分析云平台1. 项目背景与目标 (Project Introduction)本项目旨在开发一个基于云端的全栈应用，解决数据预处理门槛高、可视化成本大的痛点。核心目标：利用通义千问 AI 实现智能数据清洗，并通过线性回归模型提供预测服务 。课程合规性：项目属于 Option 1: Application Deployment，涵盖容器化、CRUD 功能及云资源管理 。2. 技术栈选型 (Technology Stack)为了符合 IaaS/PaaS 混合模型及高性能要求，选定以下组件 ：模块技术选型说明前端 (Frontend)React.js + Tailwind CSS + ECharts负责三窗口响应式布局及热力图动态渲染。后端 (Backend)Python FastAPI异步框架，天然适配 AI SDK 和数据科学库。数据库 (Database)AWS RDS (PostgreSQL)存储用户信息、加密密码及模型元数据 。对象存储 (Storage)AWS S3存储用户上传的原始 CSV/Excel 数据集 。AI/ML 引擎通义千问 API + Scikit-learn结合大模型逻辑清洗与传统机器学习预测。部署环境 (Cloud)Docker + AWS App Runner / EC2实现应用容器化与自动化云部署 。3. 规范化接口设计 (API Specification)接口设计遵循 RESTful 原则，禁止功能冗余 ：POST /auth/login: 统一登录。根据数据库返回的角色字段 (role: user/admin) 实现权限分流。POST /data/upload: 上传数据。后端接收后流式上传至 S3，并触发 AI 处理流水线。GET /data/analysis/{id}: 聚合数据中心。前端通过此接口一次性获取：AI 预处理后的数据摘要。用于 ECharts 渲染的相关性热力图矩阵。线性回归模型的训练结果（R² 分数、预测曲线点位）。GET /admin/users: 管理员专用。获取所有用户列表及账户状态 。DELETE /admin/data/{id}: 管理员专用。物理删除 S3 文件并重置关联的可视化数据。4. 核心功能逻辑伪代码 (Core Implementation)4.1 后端：数据流水线逻辑 (Python)Pythondef process_user_data(file_object):
    # 1. 接入通义千问 AI 进行数据清洗决策
    raw_df = pd.read_csv(file_object)
    ai_logic = Qwen.get_cleaning_script(raw_df.head())
    cleaned_df = raw_df.apply(ai_logic) # 执行逻辑清洗
    
    # 2. 计算热力图 (Correlation Matrix)
    heatmap_matrix = cleaned_df.corr().values.tolist()
    
    # 3. 基础线性模型预测
    model = LinearRegression().fit(cleaned_df[['feature']], cleaned_df['target'])
    accuracy = model.score(X, y) # 计算模型准确率
    
    return {
        "preview": cleaned_df.head(10).to_dict(),
        "heatmap": heatmap_matrix,
        "metrics": {"accuracy": accuracy}
    }
4.2 前端：三窗口展示组件 (React)JavaScriptconst Dashboard = ({ data }) => (
  <div className="grid grid-cols-3 gap-4 h-full">
    {/* 窗口 A: 数据预处理结果 */}
    <div className="overflow-auto border p-2"><Table data={data.preview} /></div>
    
    {/* 窗口 B: 热力图可视化 */}
    <div className="border p-2"><ECharts option={getHeatmapOption(data.heatmap)} /></div>
    
    {/* 窗口 C: 模型预测结果 */}
    <div className="border p-2">
      <LineChart score={data.metrics.accuracy} />
      <p className="text-center font-bold">Accuracy: {data.metrics.accuracy}</p>
    </div>
  </div>
);